import React from "react";

const AddProps = (props) => {
  return (
    <h2>
      Cricketer name is: {props.name} & <br /> public name is : {props.PName}
    </h2>
  );
};

export default AddProps;
